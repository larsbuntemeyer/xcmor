name: xcmor_test
channels:
  - conda-forge
dependencies:
  - pytest-cov
  - pytest
  - pytest-xdist
  - netcdf4
  - lxml
  - pandas
  - pint #<=0.23
  - pint-xarray
  - pooch
  - rich
  - pooch
  - xarray
  - pyyaml
  - cf_xarray
  - pip
  - pip:
      - pytest-pretty
